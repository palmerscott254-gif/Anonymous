{
  "name": "ghostchat-backend",
  "version": "0.0.1",
  "type": "module",
  "description": "GhostChat encrypted messaging server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "npm install --prefix .. --omit=dev --package-lock=false --ignore-scripts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.0",
    "express": "^4.18.2",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.11.3",
    "bcryptjs": "^3.0.3",
    "zod": "^4.4.3",
    "socket.io": "^4.8.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
